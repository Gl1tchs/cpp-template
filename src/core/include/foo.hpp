#pragma once

#include <cstdint>

namespace foo {

/// @brief Calculates factorial of given number
/// @param number Number to calculate to
/// @return Factorial of number
uint32_t factorial(uint32_t number);

}  // namespace foo
